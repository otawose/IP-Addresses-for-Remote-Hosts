#This script will list the IP address for a remote host

$RemoteHost = "www.classtest.com"

$IPAddress = [System.Net.Dns]::GetHostAddresses($RemoteHost)

Write-Host "IP address of $RemoteHost is $IPAddress"
